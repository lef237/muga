package app::std_path_with_file_name

import std::path

fn main(): String {
  source = "reports/daily.txt".path::from_string()
  output = source.path::with_file_name("summary.txt")
  output.path::as_string()
}
