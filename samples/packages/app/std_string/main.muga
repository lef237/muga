package app::std_string

import std::string

fn main(): String {
  empty: List[String] = []
  prefix = empty.string::join(",")
  rendered = ["Muga", 8080.to_string(), true.to_string()].string::join("|")
  [prefix, "config ", rendered].string::concat_all()
}
