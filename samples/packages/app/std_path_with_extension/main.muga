package app::std_path_with_extension

import std::path

fn main(): String {
  source = "reports/daily.txt".path::from_string()
  output = source.path::with_extension("json")
  output.path::as_string()
}
