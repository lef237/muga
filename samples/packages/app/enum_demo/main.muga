package app::enum_demo

import util::states

fn main(): Int {
  status: states::Status[Int] = states::Status::Ready(7)
  match status {
    states::Status::Ready(value) => value
    states::Status::Waiting => 0
    states::Status::Failed(message) => 0
  }
}
