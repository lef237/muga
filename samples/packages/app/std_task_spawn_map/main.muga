package app::std_task_spawn_map

import std::task
import std::list

fn work(n: Int): Int {
  n * 2
}

fn main(): Int {
  items = [1, 2, 3, 4, 5]
  results = task::spawn_map(items, work)
  list::fold(results, 0, fn(total, item) { total + item })
}
