package app::std_fs_file_size

import std::fs
import std::path

fn main(): String {
  file_path = "samples/packages/app/std_hash/payload.bin".path::from_string()
  match file_path.fs::file_size_path() {
    Result::Ok(size) => size.to_string()
    Result::Err(error) => error.operation
  }
}
