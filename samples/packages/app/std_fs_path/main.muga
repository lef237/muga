package app::std_fs_path

import std::fs
import std::path

fn main(): String {
  missing = "samples/packages/app/std_fs_path/missing.txt".path::from_string()
  match missing.fs::read_text_path() {
    Result::Ok(text) => text
    Result::Err(error) => error.operation
  }
}
