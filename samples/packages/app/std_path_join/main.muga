package app::std_path_join

import std::path

fn main(): String {
  base = "samples/packages/app".path::from_string()
  joined = base.path::join("std_path_join/main.muga")
  joined.path::as_string()
}
