package app::std_env_current_dir

import std::env
import std::path

fn main(): String {
  match env::current_dir() {
    Result::Ok(dir) => dir.path::is_absolute().to_string()
    Result::Err(error) => error.operation
  }
}
