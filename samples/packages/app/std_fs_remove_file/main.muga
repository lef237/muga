package app::std_fs_remove_file

import std::fs
import std::path

fn main(): String {
  missing = path::from_string("samples/packages/app/std_fs_remove_file/missing.txt")
  match fs::remove_file_path(missing) {
    Result::Ok(value) => "removed"
    Result::Err(error) => error.operation
  }
}
