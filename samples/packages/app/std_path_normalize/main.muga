package app::std_path_normalize

import std::path

fn main(): String {
  source = "reports/./2026/../daily.txt".path::from_string()
  normalized = source.path::normalize()
  normalized.path::as_string()
}
