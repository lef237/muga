package app::std_fs_remove_dir

import std::fs
import std::path

fn main(): String {
  missing = path::from_string("samples/packages/app/std_fs_remove_dir/missing")
  match fs::remove_dir_path(missing) {
    Result::Ok(value) => "removed"
    Result::Err(error) => error.operation
  }
}
